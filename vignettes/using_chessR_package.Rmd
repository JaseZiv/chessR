---
title: "Using the chessR Package"
author: "Jason Zivkovic"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Using the chessR Package}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```


```{r gh-installation, eval = FALSE, echo = TRUE}
# install.packages("devtools")
# devtools::install_github("JaseZiv/chessR")
```

# Extracting raw chess game data

```{r libraries, message=FALSE, warning=FALSE}
library(chessR)
library(ggplot2)
library(dplyr)
```

To extract raw chess game data in a data frame, use the `get_raw_game_data()` function. This function either accepts one player's username, or a vector of multiple users. 

```{r get_raw_data, message=FALSE, warning=FALSE}
raw_chess <- get_raw_game_data("JaseZiv")
```


```{r inspect_raw, message=FALSE, warning=FALSE}
glimpse(raw_chess)
```



